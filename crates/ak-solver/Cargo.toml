[package]
name = "ak-solver"
description = "Assignment solvers (exhaustive, simulated annealing) over ak-eval — Layer 5, not yet implemented"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
ak-domain = { workspace = true }
ak-eval = { workspace = true }

[lints]
workspace = true
